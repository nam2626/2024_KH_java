Object 클래스
	자바에서 모든 클래스의 최상위 클래스.
	클래스가 가져야될 기본적인 메서드를 담고 있음.
	
1. toString() : 객체를 문자열로 표현하는 메서드
2. hashCode() : 객체를 식별하는데 사용되는 정수값
3. equals(Object) : 두 객체가 내부 내용이 동일한지 비교하는 메서드
4. clone() : 객체를 복제하는 메서드
5. getClass() : 객체의 클래스를 나타내는 메서드(클래스 명, 패키지...)
6. finalize() : 객체가 GC에 수거되기 전에 호출되는 메서드

	